{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray",
  "description": "What the tray panel may ask the shell to do: list what it shows, draw each diagram from a handle or draft id the shell gave out, fit its own height, and choose one of the tray's own items. Nothing that writes, and nothing from the app menu.",
  "windows": ["tray"],
  "permissions": [
    "allow-tray-panel",
    "allow-tray-choose",
    "allow-tray-panel-fit",
    "allow-peek-document",
    "allow-recovery-read"
  ]
}
