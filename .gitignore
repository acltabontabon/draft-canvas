node_modules
dist
dist-desktop
dist-ssr
coverage
*.local
.DS_Store
.vite
playwright-report
playwright-report-offline
test-results
.playwright
*.tsbuildinfo

# Disposable benchmark run output (benchmark/baseline.json and
# benchmark/memory-chart.svg are committed on purpose — see docs/reference/performance.md)
benchmark/results/

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.crt

# Editors
.idea
.vscode
# ...except the VS Code extension's launch config, which is how you run it (F5)
!/vscode-extension/.vscode/

# VS Code extension build output and packages
vscode-extension/out/
vscode-extension/.vscode-test/
*.vsix

# Desktop app (src-tauri/): cargo output, and what Tauri regenerates on every build (the schemas,
# and one permission per command from build.rs's list)
src-tauri/target/
src-tauri/gen/
src-tauri/permissions/autogenerated/
playwright-report-desktop

# Local agent tooling
.claude/
